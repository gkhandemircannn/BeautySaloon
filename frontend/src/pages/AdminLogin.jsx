import { useState } from 'react'; import { useNavigate } from 'react-router'; import { loginAdmin } from '../services/authApi'; import './AdminLogin.css'
function AdminLogin(){const nav=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');async function submit(e){e.preventDefault();try{await loginAdmin(email,password);nav('/admin')}catch{setError('E-posta adresi veya şifre hatalı.')}}return <main className="login"><form onSubmit={submit}><p>Lumi Beauty</p><h1>Yönetici Girişi</h1><label>E-posta<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Şifre<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<small>{error}</small>}<button>Giriş Yap</button></form></main>};export default AdminLogin
